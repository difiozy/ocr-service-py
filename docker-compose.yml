version: "3"

services:
  ocr:
    image: "final_stage_build_ocr-parse_service:latest"
    expose:
      - "5000"

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - ocr
    ports:
      - "4000:4000"